



work with bits maybe 00 01 10 11
struct instruction contains 
opcpde, num, origin,src

absolute maybe numbers
if first word is filed and ther is : so its label
if there is no : so its command - which operands she can get
if the first char in arg is r- register if the number between 1-7 if yes its register if above its label








void moreLength(int x) {
	cmd_list.length += x;}
